module.exports = '(' + debug.toString() + ')()'

function debug () {
  var _s = Error().stack
  var _c = arguments.callee.caller.toString()

  _debug()

  function _debug () {
    var readlineSync = require('readline-sync')
    var input = readlineSync.question('repl: ')

    if (input !== 'c') {
      try {
        console.log(eval(input))
      } catch (e) {
        console.log(input, 'is not defined.')
      }
      _debug()
    }
  }
}

